version: 2

sources:
  - name: snowflake_raw  # This is a nickname for your source
    database: DEV_DB     # The actual Database name in Snowflake
    schema: HR       # The actual Schema name in Snowflake
    tables:
      - name: employees
      - name: departments